// Contains utility functions to work with strings
package stringutils

// Reverse returns its argument string reversed rune-wise from left to right
func Reverse(s string) string {
	return reverseTwo(s)
}
